class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int ans=0;
        for (int i:nums){
            ans=ans^i; // 0^ n is always n so ans=0 is initialized
        }

        return ans;
        
    }
};
